# Cursor indexing — keep AI context small and CPU low

node_modules/
.next/
dist/
build/
coverage/
# public/uploads/ — не скрывать от инструментов: иначе `git add .` из Cursor может не увидеть каталог. Тяжёлые ассеты скрыты в VS Code (files.exclude).

# Extra heavy / generated
out/
.turbo/
.cache/
.vercel/
package-lock.json
pnpm-lock.yaml
*.tsbuildinfo
